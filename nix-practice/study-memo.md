# Nix入門

# Nix言語
Nixは純粋関数型のドメイン固有言語(DSL)

- 純粋関数型: 副作用がなく、同じ入力に対して常に同じ出力となる。
- 遅延評価: 必要なときにのみ値を評価する。(逆を言えば、必要なときにのみ値を生成する

???
- 汎用ではない: DSL言語のため、パッケージやビルド、システム構成の記述に特化した言語である。

???
Nix言語の評価
ソースコード->AST->評価